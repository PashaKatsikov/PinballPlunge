/// Microsoft Clarity project id — unique per project ([FINGERPRINT]).
/// Never reuse this id across other apps.
/// Replace the placeholder with the real id from clarity.microsoft.com.
const String kClarityProjectId = 'xmd2n8gh7o';
